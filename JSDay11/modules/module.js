const caculateLength = (str) => {
  console.log(str.length);
};

const checkLengthMin10 = (str) => {
  console.log(str.length > 10);
};

const moduleCheck = { caculateLength, checkLengthMin10 };

export default moduleCheck;
